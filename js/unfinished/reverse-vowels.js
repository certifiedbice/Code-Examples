function reverseVowels(input) {
  let vowelHash={'a':'a','e':'e','i':'i','o':'o','u':'u'};
  
  let a;
  let b;
  
  input.split();
  
  input.forEach((el,idx)=>{
     
    if(idx<=input.length/2){
       if(vowelHash[el]){
         a=el;
         
         
       }
    }
    
    
    
  });
  
  return result;
}