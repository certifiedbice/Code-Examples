import React, { Component } from 'react';
// import Context from '../../context/Context';
// import './List.css';

export default class List extends Component{
	
	// static contextType = Context;

	render() {
		// let x = this.context.fileList;
		// console.log(x);
		return (
			<div>
			{
				// listFiles.forEach((script, index) => { console.log(script); })
				// list.map((script, index) => list[index])
			}
			</div>
		);
	}
}